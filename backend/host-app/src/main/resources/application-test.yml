spring:
  flyway:
    enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  datasource:
    url: jdbc:h2:mem:labelhub-test
    username: test
    password: test

labelhub:
  auth:
    mode: memory
  internal-token: test-internal-token
  security:
    aes-key: test-aes-key-32bytes-padding!!
  agent:
    base-url: http://localhost:8000
  storage:
    minio:
      endpoint: http://localhost:9000
      access-key: test
      secret-key: test
